table:
  schema: public
  name: roles
array_relationships:
  - name: account_roles
    using:
      foreign_key_constraint_on:
        column: role
        table:
          schema: public
          name: account_roles
  - name: invites
    using:
      foreign_key_constraint_on:
        column: role
        table:
          schema: public
          name: invites
