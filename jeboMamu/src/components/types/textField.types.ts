export enum Type {
  text = 'text',
  password = 'password',
}

export enum Size {
  small = 'small',
  normal = 'normal',
  large = 'large',
}

export enum Variant {
  default = 'default',
  minimalistic = 'minimalistic',
}
